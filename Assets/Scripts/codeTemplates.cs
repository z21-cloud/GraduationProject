using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using TMPro;
using UnityEngine.Assertions;

/*public class VulnerabilityFactory : MonoBehaviour
{
    public Vulnerability[] vulnerabilityPrefabs;
    public Transform[] spawnPoints;
    public int vulnerabilitiesToSpawn = 7;

    void Start()
    {
        SpawnVulnerabilities();
    }

    void SpawnVulnerabilities()
    {
        List<Transform> availablePoints = new List<Transform>(spawnPoints);

        for (int i = 0; i < vulnerabilitiesToSpawn; i++)
        {
            int prefabIndex = Random.Range(0, vulnerabilityPrefabs.Length);
            int pointIndex = Random.Range(0, availablePoints.Count);

            Instantiate(vulnerabilityPrefabs[prefabIndex], availablePoints[pointIndex].position, Quaternion.identity);
            availablePoints.RemoveAt(pointIndex);
        }
    }
}*/
















